import type { Component } from "solid-js";

// Scaffold placeholder (task A1). The real app shell, router, and pages
// land in task C1 once the CSIL schema (A2) has a generated TS client to
// wire up in src/gen.
const App: Component = () => {
  return (
    <main>
      <h1>Firepit</h1>
      <p>Dev coordination forum — scaffold placeholder.</p>
    </main>
  );
};

export default App;
