name: Firepit Release Tag
description: "Calculate and push the next release tag after a merge."
on:
  events:
    - pull_request_merged
  branches:
    - main
jobs:
  tag:
    job_file: release-control.yaml
    environment:
      REACTORCIDE_FIREPIT_JOB: tag
