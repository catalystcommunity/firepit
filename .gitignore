# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work
go.work.sum
/api/firepit-api

# Node / webapp (webapp/ also has its own .gitignore; duplicated here so
# root-level tooling that walks the whole tree doesn't pick these up either)
node_modules/
dist/
dist-ssr/
*.local

# Generated code under webapp/src/gen, api/internal/csil, and clients/ is
# CHECKED IN (task A2 wires `./tools.sh gen`; see csil/firepit.csil) — do not
# ignore it. clients/typescript is a standalone npm package generated
# alongside the others; ignore only its own build byproducts.
clients/typescript/node_modules/
clients/typescript/package-lock.json

# IDE / editor
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.dev

# Local dev runtime
/logs/
.claude/
/set-secrets.sh
