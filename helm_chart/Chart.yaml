apiVersion: v2
name: firepit
description: A dev coordination forum for open source projects — threaded discussion, endorsements, and GitHub event ingestion
type: application

# The trusted Reactorcide release plugin updates this value with
# version/VERSION.txt after it creates a release tag.
version: 0.4.1

# This application version matches version/VERSION.txt.
appVersion: "0.4.1"
